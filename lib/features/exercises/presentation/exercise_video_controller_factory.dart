import 'package:video_player/video_player.dart';

VideoPlayerController createExerciseVideoController({
  required String remoteUrl,
  String? localPath,
}) => throw UnsupportedError('Platform video controller is not configured.');
